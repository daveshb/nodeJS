# Notas

Este es mi programa en Node

```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     Esta es la base a multiplicar               [number] [required]
  -l, --listar   l para listar la tabla en clg        [boolean] [default: false]
  -h, --hasta    hasta donde multiplicar                   [number] [default: 5]
```
